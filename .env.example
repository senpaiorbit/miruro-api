# .env.example — copy to .env and fill in values

# Optional: comma-separated list of allowed origins.
# Leave empty or set to * to allow all origins.
ALLOWED_ORIGINS=https://www.miruro.tv,https://miruro.to

# Optional: secret API key. If set, requests must include x-api-key header.
API_KEY=your_secret_key_here
